spring:
  datasource:
    url: ${DATABASE_URL:jdbc:sqlite:./money-subscriptions-dev.sqlite}
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.community.dialect.SQLiteDialect
  flyway:
    placeholders:
      uuid_type: TEXT
      money_type: NUMERIC

logging:
  level:
    com.moneysubscriptions: DEBUG
